import { createSelector } from 'reselect';

/**
 * Direct selector to the passwordResetContainer state domain
 */
const selectPasswordResetPage = (state) => state.get('passwordResetPage');

/**
 * Other specific selectors
 */


/**
 * Default selector used by PasswordResetContainer
 */


export {
  selectPasswordResetPage,
};
